    function concatena(str1, str2) {
        var result = '';
        // Adiciona os caracteres da primeira string
        for (var i = 0; i < str1.length; i++) {
            result += str1[i];
        }
